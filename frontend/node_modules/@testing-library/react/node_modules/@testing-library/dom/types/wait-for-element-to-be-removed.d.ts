import {waitForOptions} from '@testing-library/react/node_modules/@testing-library/dom/types/wait-for'

export function waitForElementToBeRemoved<T>(
  callback: T | (() => T),
  options?: waitForOptions,
): Promise<void>
